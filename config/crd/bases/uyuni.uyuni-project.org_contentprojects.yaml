---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: contentprojects.uyuni.uyuni-project.org
spec:
  group: uyuni.uyuni-project.org
  names:
    kind: ContentProject
    listKind: ContentProjectList
    plural: contentprojects
    singular: contentproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.label
      name: Label
      type: string
    - jsonPath: .status.buildStatus
      name: Build
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              build:
                properties:
                  autoBuildSources:
                    description: Trigger a build when source channels change.
                    type: boolean
                  message:
                    default: automated build by uyuni-operator
                    type: string
                  schedule:
                    description: Cron expression for periodic builds. Standard 5-field
                      format.
                    type: string
                type: object
              description:
                type: string
              environments:
                description: Environments are now managed via separate ClmEnvironment
                  CRDs
                items:
                  properties:
                    description:
                      type: string
                    label:
                      pattern: ^[a-z0-9-]+$
                      type: string
                    name:
                      type: string
                    predecessor:
                      description: Predecessor in the promotion chain. Empty for root.
                      type: string
                  required:
                  - label
                  - name
                  type: object
                type: array
              filters:
                items:
                  properties:
                    criteria:
                      properties:
                        field:
                          enum:
                          - name
                          - nevr
                          - nevra
                          - provides_name
                          - synopsis
                          - advisory_type
                          - advisory_name
                          type: string
                        matcher:
                          enum:
                          - equals
                          - matches
                          - contains
                          - greater
                          - lower
                          type: string
                        value:
                          type: string
                      required:
                      - field
                      - matcher
                      - value
                      type: object
                    name:
                      type: string
                    rule:
                      default: deny
                      enum:
                      - allow
                      - deny
                      type: string
                    type:
                      enum:
                      - package
                      - errata
                      - module
                      - appstream
                      - ptf
                      type: string
                  required:
                  - criteria
                  - name
                  - rule
                  - type
                  type: object
                type: array
              label:
                description: Immutable after creation.
                pattern: ^[a-z0-9-]+$
                type: string
              name:
                type: string
              organizationRef:
                description: |-
                  LocalObjectRef is a name-only reference to a CR in the same namespace.
                  We deliberately don't include namespace or kind: cross-namespace refs
                  complicate RBAC and aren't needed in our model, and each ref's kind is
                  implied by the field where it appears.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              sourceRefs:
                items:
                  description: |-
                    LocalObjectRef is a name-only reference to a CR in the same namespace.
                    We deliberately don't include namespace or kind: cross-namespace refs
                    complicate RBAC and aren't needed in our model, and each ref's kind is
                    implied by the field where it appears.
                  properties:
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - label
            - name
            - organizationRef
            type: object
          status:
            properties:
              attachedSources:
                items:
                  type: string
                type: array
              buildStatus:
                enum:
                - Idle
                - Building
                - Failed
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              environmentStates:
                items:
                  properties:
                    builtAt:
                      format: date-time
                      type: string
                    builtVersion:
                      type: integer
                    derivedChannels:
                      items:
                        type: string
                      type: array
                    label:
                      type: string
                    name:
                      type: string
                  required:
                  - label
                  type: object
                type: array
              filterIds:
                additionalProperties:
                  type: integer
                type: object
              lastBuildActionId:
                type: integer
              lastBuildSourceFingerprint:
                type: string
              lastBuildStartedAt:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              uyuniId:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
