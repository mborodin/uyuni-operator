---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clmenvironments.uyuni.uyuni-project.org
spec:
  group: uyuni.uyuni-project.org
  names:
    kind: ClmEnvironment
    listKind: ClmEnvironmentList
    plural: clmenvironments
    singular: clmenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.id
      name: Id
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.projectRef.name
      name: Project
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cluster:
                description: Reference to UyuniProvider (cluster). Nil = use default
                  provider
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              description:
                type: string
              id:
                description: Immutable after creation. Environment label in Uyuni
                  (the "id")
                pattern: ^[a-z0-9-]+$
                type: string
              name:
                description: Human-readable environment name (mutable)
                type: string
              organizationRef:
                description: Reference to Organization (may be inherited from project)
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              predecessor:
                description: Label of predecessor environment in promotion chain (empty
                  = root)
                type: string
              projectRef:
                description: Reference to parent ContentProject (immutable)
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
            required:
            - id
            - name
            - projectRef
            type: object
          status:
            properties:
              builtVersion:
                description: Built version number
                type: integer
              conditions:
                description: Reconciliation conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              state:
                description: Current environment state in Uyuni
                type: string
              uyuniLabel:
                description: Environment ID assigned by Uyuni (matches spec.id when
                  reconciled)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
